export interface BDIModule {
    module_id: number;
    description: string;
    address: string;
}

export interface Driver {
    module_id: number;
    description: string;
    address: string;
}

export interface TestSource {
    module_id: number;
    description: string;
    address: string;
};